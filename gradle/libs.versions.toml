[versions]
loom = "1.4-SNAPSHOT"
architecturyPlugin = "3.4-SNAPSHOT"
preprocessor = "fad42fb"
shadow = "7.1.2"
blossom = "1.3.1"

[plugins]
loom = { id = "dev.architectury.loom", version.ref = "loom" }
architecturyPlugin = { id = "architectury-plugin", version.ref = "architecturyPlugin" }
preprocessor = { id = "com.replaymod.preprocess", version.ref = "preprocessor" }
preprocessorRoot = { id = "com.replaymod.preprocess-root", version.ref = "preprocessor" }
shadow = { id = "com.github.johnrengelman.shadow", version.ref = "shadow" }
blossom = { id = "net.kyori.blossom", version.ref = "blossom" }