{
  "schemaVersion": 1,
  "id": "${id}",
  "name": "${name}",
  "version": "${version}",
  "description": "${description}",
  "license": "${license}",
  "authors": [
    "Example Author"
  ],
  "mixins": [
    "${id}.mixins.json"
  ],
  "environment": "*",
  "depends": {
    "minecraft": "${mcVersionFabric}"
  }
}